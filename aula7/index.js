// Não podemos criar constantes com nomes reservados
// Constantes precisam ter nomes significativos
// Não pode começar nome de constantes com número
// Nome de constante não pode conter espaços ou hífen
// Utilizamos camelCase nas constantes
// Case-sensitive
// Não podemos mudar o valor de uma constante
// É preciso atribuir o valor a constante na atribuição
// Não utiliza VAR utilize CONST

// - + * /

const nome = "João";

console.log(nome);

const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
let resultadoTriplicado = resultado * 3;
resultadoTriplicado = resultadoTriplicado + 5;

console.log(resultadoTriplicado);
console.log(primeiroNumero);
console.log(typeof(primeiroNumero));

